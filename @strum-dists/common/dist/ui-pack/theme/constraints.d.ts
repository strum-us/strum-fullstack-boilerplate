export declare namespace relative {
    const widthFull: any;
    const position: any;
    const attachRight: (amount?: string) => any;
    const attachLeft: (amount?: string) => any;
    const attachTop: (amount?: string) => any;
    const attachBottom: (amount?: string) => any;
    const fixedHeightFullWidth: (height: string) => any;
    const widthCenterAlign: (width: string) => any;
    const leftSpacing: (left: string, width?: string) => any;
    const heightFull: any;
    const heightCenterAlign: (height: string) => any;
    const circleVerticalCenter: (length: string) => any;
    const circle: (length: string) => any;
    const boxVerticalCenter: (length: string) => any;
}
export declare const basisHeight: {
    auto: string;
    small: string;
    normal: string;
    large: string;
};
